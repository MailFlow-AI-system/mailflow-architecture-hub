# Private client documents

This directory is reserved for private commercial documents. Its contents are not part of the public application and must not be imported by Astro, copied into `public/`, or included in `dist/`.

The approved client `.docx` document will be added here later in a separate workflow. Do not commit private client artifacts or expose them through public routes.
