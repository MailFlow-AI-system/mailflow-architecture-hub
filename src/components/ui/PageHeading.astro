---
import Breadcrumbs from '../shell/Breadcrumbs.astro';

interface Props {
  eyebrow?: string;
  title: string;
  description?: string;
  breadcrumbs?: { label: string; href?: string }[];
  wideTitle?: boolean;
}

const { eyebrow, title, description, breadcrumbs, wideTitle = false } = Astro.props;
---

<div class:list={['page-heading', { 'page-heading--wide-title': wideTitle }]}>
  {breadcrumbs && <Breadcrumbs items={breadcrumbs} />}
  {eyebrow && <p class="eyebrow">{eyebrow}</p>}
  <h1>{title}</h1>
  {description && <p class="page-heading__description">{description}</p>}
</div>

<style>
  .page-heading {
    padding: var(--space-7) 0 var(--space-6);
  }

  .page-heading :global(.breadcrumbs) {
    margin-bottom: var(--space-5);
  }

  .page-heading h1 {
    max-width: 14ch;
    margin: 0;
    color: var(--color-paper-100);
    font-family: var(--font-display);
    font-size: clamp(3rem, 8vw, 6.5rem);
    font-weight: 800;
    letter-spacing: var(--tracking-tight);
    line-height: 0.86;
    text-transform: uppercase;
  }

  .page-heading--wide-title h1 {
    max-width: 22ch;
  }

  .page-heading__description {
    max-width: 42rem;
    margin: var(--space-4) 0 0;
    color: var(--color-paper-300);
    font-size: var(--text-lg);
  }

  @media (max-width: 42rem) {
    .page-heading h1 {
      max-width: 100%;
      font-size: clamp(1.9rem, 9vw, 3.75rem);
    }
  }
</style>
